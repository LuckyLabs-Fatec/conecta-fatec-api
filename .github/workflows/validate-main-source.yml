name: Validate master source branch

on:
  pull_request:
    branches:
      - master

jobs:
  validate-source-branch:
    runs-on: ubuntu-latest

    steps:
      - name: Allow only develop into master
        run: |
          if [ "${{ github.head_ref }}" != "develop" ]; then
            echo "A branch master só pode receber PR vindo da develop."
            echo "Branch atual: ${{ github.head_ref }}"
            exit 1
          fi